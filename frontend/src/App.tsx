import { Button } from 'antd'

function App() {
  return (
    <main>
      <Button
        type="primary"
        style={{ position: 'fixed', left: 24, top: 24 }}
      >
        Start
      </Button>
    </main>
  )
}

export default App
